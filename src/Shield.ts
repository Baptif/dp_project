export default class Shield {
    turnOn(): void {
        // A set of instructions to run
        console.log('🟢 Shield turned ON 🟢')
    }

    turnOff(): void {
        // A set of instructions to run
        console.log('🔴 Shield turned OFF 🔴')
    }
}